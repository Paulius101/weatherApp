import { MapSelectDirective } from './map-select.directive';

describe('MapSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new MapSelectDirective();
    expect(directive).toBeTruthy();
  });
});
